/**
 * Режим анонса: сайт закрыт на пересборку под редизайн (ветка `sandwor_v2`),
 * вместо любой страницы отдаётся тизер новой версии с контактами.
 *
 * Флаг build-time, а не переменная окружения: middleware бандлится в edge-рантайм,
 * где `process.env` инлайнится на сборке — правка `.env.production` на сервере
 * ни на что не влияет. Включение/выключение — только коммит + деплой.
 */
export const IS_SITE_CLOSED: boolean = true;

/** Служебный маршрут тизера. Все запросы переписываются на него. */
export const SOON_PATH = '/soon';

/**
 * Заголовок с локалью, вычисленной middleware из исходного пути.
 * Тизер живёт вне сегмента `[locale]`, поэтому next-intl сам локаль не определит.
 */
export const SOON_LOCALE_HEADER = 'x-soon-locale';

/** `Retry-After` в секундах — подсказка поисковикам, когда вернуться. */
export const SOON_RETRY_AFTER_SECONDS = 3600;
